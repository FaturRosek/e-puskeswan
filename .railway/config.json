{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-interaction --no-dev --optimize-autoloader --no-scripts && npm run build"
  },
  "deploy": {
    "startCommand": "chmod -R 775 bootstrap/cache storage && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache && vendor/bin/heroku-php-apache2 public/"
  }
}
